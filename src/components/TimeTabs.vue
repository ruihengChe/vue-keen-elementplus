<template>
    <div></div>
    <!-- <div class="time-tabs" :class="size">
        <span v-for="item in dataArr" :key="item[valueLabel]"
        :class="{ actived: activedKey === item[valueLabel]}"
        @click.stop="tabClick(item)">
        <i v-if="item.icon" :class="item.icon" class="icon-color">
            {{ iten[nameLabel] }}
        </i>
    </span>
    </div> -->
</template>